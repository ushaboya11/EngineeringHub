#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int i = 1, sum = 0;
    while (i <= n) {
        if (i % 2 == 0) {
            sum += i;
        }
        i++;
    }

    printf("%d\n", sum);
    return 0;
}
